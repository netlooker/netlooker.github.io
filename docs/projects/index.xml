<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>Projects on Netlooker&#39;s Notes</title>
    <link>http://netlooker.github.io/projects/</link>
    <description>Recent content in Projects on Netlooker&#39;s Notes</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>en</language>
    <lastBuildDate>Sun, 29 Mar 2026 14:24:52 +0000</lastBuildDate><atom:link href="http://netlooker.github.io/projects/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>Synapse</title>
      <link>http://netlooker.github.io/projects/synapse/</link>
      <pubDate>Sun, 29 Mar 2026 14:24:52 +0000</pubDate>
      
      <guid>http://netlooker.github.io/projects/synapse/</guid>
      <description>&lt;p&gt;Synapse is a semantic cortex for markdown archives. It crawls a folder of notes, carves the signal into chunks, embeds the note stream, and maps hidden links that plain keyword search leaves buried in the subnet. The mission is simple: turn a loose markdown vault into a live retrieval layer that can surface weak signals, broken graph edges, and connections the human eye misses under load.&lt;/p&gt;
&lt;h2 id=&#34;-tech-stack&#34;&gt;// TECH STACK&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;Python 3.12&lt;/code&gt; — runs the indexing, search, discovery, and transport daemons.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;sqlite-vec&lt;/code&gt; — stores local vector memory in a lightweight SQLite-backed layer.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Perplexity embeddings&lt;/code&gt; — power note-level and contextual chunk-level retrieval.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Infinity&lt;/code&gt; — serves the high-end &lt;code&gt;4B&lt;/code&gt; embedding models over a local-compatible endpoint.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Ollama&lt;/code&gt; — provides fallback local embeddings and reasoning-model access on silicon.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;FastAPI&lt;/code&gt; — exposes Synapse over HTTP and OpenAPI for web clients and PWAs.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;MCP&lt;/code&gt; — exposes the same tool surface to agent runtimes.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;PydanticAI&lt;/code&gt; — drives &lt;code&gt;Cipher&lt;/code&gt;, the reasoning shell over the deterministic core.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;-architecture&#34;&gt;// ARCHITECTURE&lt;/h2&gt;
&lt;p&gt;The stack runs in three bands. The retrieval core scans markdown, extracts metadata, chunks notes, writes vectors, and scores search and discovery results with deterministic Python services. &lt;code&gt;Cipher&lt;/code&gt; rides above that layer and handles audit, explanation, and maintenance review without taking over the mechanics. On the edge, MCP and HTTP both hit the same service layer, so agents and apps talk to one grid instead of two drifting clones.&lt;/p&gt;</description>
      <content>&lt;p&gt;Synapse is a semantic cortex for markdown archives. It crawls a folder of notes, carves the signal into chunks, embeds the note stream, and maps hidden links that plain keyword search leaves buried in the subnet. The mission is simple: turn a loose markdown vault into a live retrieval layer that can surface weak signals, broken graph edges, and connections the human eye misses under load.&lt;/p&gt;
&lt;h2 id=&#34;-tech-stack&#34;&gt;// TECH STACK&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;Python 3.12&lt;/code&gt; — runs the indexing, search, discovery, and transport daemons.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;sqlite-vec&lt;/code&gt; — stores local vector memory in a lightweight SQLite-backed layer.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Perplexity embeddings&lt;/code&gt; — power note-level and contextual chunk-level retrieval.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Infinity&lt;/code&gt; — serves the high-end &lt;code&gt;4B&lt;/code&gt; embedding models over a local-compatible endpoint.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Ollama&lt;/code&gt; — provides fallback local embeddings and reasoning-model access on silicon.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;FastAPI&lt;/code&gt; — exposes Synapse over HTTP and OpenAPI for web clients and PWAs.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;MCP&lt;/code&gt; — exposes the same tool surface to agent runtimes.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;PydanticAI&lt;/code&gt; — drives &lt;code&gt;Cipher&lt;/code&gt;, the reasoning shell over the deterministic core.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;-architecture&#34;&gt;// ARCHITECTURE&lt;/h2&gt;
&lt;p&gt;The stack runs in three bands. The retrieval core scans markdown, extracts metadata, chunks notes, writes vectors, and scores search and discovery results with deterministic Python services. &lt;code&gt;Cipher&lt;/code&gt; rides above that layer and handles audit, explanation, and maintenance review without taking over the mechanics. On the edge, MCP and HTTP both hit the same service layer, so agents and apps talk to one grid instead of two drifting clones.&lt;/p&gt;
&lt;h2 id=&#34;-status&#34;&gt;// STATUS&lt;/h2&gt;
&lt;p&gt;Synapse is live and usable. It indexes generic markdown folders, stores note and chunk embeddings, runs semantic search in &lt;code&gt;note&lt;/code&gt;, &lt;code&gt;chunk&lt;/code&gt;, and &lt;code&gt;hybrid&lt;/code&gt; modes, and exposes aligned MCP and HTTP interfaces. A built-in &lt;code&gt;synapse-smoke&lt;/code&gt; command now lets agents dry-run provider wiring, indexing, retrieval, validation, and maintenance checks against a fixture vault using a fresh temporary DB by default. The current lane focuses on tuning discovery quality on larger corpora, expanding vector-index audit inside &lt;code&gt;Cipher&lt;/code&gt;, and preparing the seam for a second backend beyond &lt;code&gt;sqlite-vec&lt;/code&gt;.&lt;/p&gt;
&lt;h2 id=&#34;-deploy&#34;&gt;// DEPLOY&lt;/h2&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre tabindex=&#34;0&#34; style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;&#34;&gt;&lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;uv sync
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;cp config/synapse.example.toml config/synapse.toml
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;uv run synapse-smoke --config config/synapse.toml
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;uv run synapse-index --config config/synapse.toml --cortex ~/notes --db ~/notes/.synapse.sqlite
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;uv run synapse-search --config config/synapse.toml --db ~/notes/.synapse.sqlite --mode hybrid &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;weak signals across notes&amp;#34;&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;uv run synapse-discover --config config/synapse.toml --db ~/notes/.synapse.sqlite --threshold 0.20 --max &lt;span style=&#34;color:#ae81ff&#34;&gt;10&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;Jack in, dry-run the stack, index the vault, and let the grid start whispering back.&lt;/p&gt;
</content>
    </item>
    
    <item>
      <title>Nyx Cortex</title>
      <link>http://netlooker.github.io/projects/nyx-cortex/</link>
      <pubDate>Sun, 29 Mar 2026 00:00:00 +0000</pubDate>
      
      <guid>http://netlooker.github.io/projects/nyx-cortex/</guid>
      <description>&lt;p&gt;Nyx is a self-sovereign AI cortex. Clone it, configure it, bake it, run it — four moves and your agent is live on your own hardware. OpenClaw handles the intelligence layer, Telegram handles the uplink, and Nix handles the build: every binary in the image is pinned to a cryptographic hash so the toolchain is identical on every machine, every time. No subscriptions. No cloud dependency. No data exfiltration. The corporate grid has no reach here.&lt;/p&gt;</description>
      <content>&lt;p&gt;Nyx is a self-sovereign AI cortex. Clone it, configure it, bake it, run it — four moves and your agent is live on your own hardware. OpenClaw handles the intelligence layer, Telegram handles the uplink, and Nix handles the build: every binary in the image is pinned to a cryptographic hash so the toolchain is identical on every machine, every time. No subscriptions. No cloud dependency. No data exfiltration. The corporate grid has no reach here.&lt;/p&gt;
&lt;h2 id=&#34;-tech-stack&#34;&gt;// TECH STACK&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;OpenClaw&lt;/strong&gt; — the AI agent daemon, runs inside the container, exposes a gateway on &lt;code&gt;:18789&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;qwen-coder&lt;/strong&gt; — local code-focused model, wired into OpenClaw for inference tasks&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Nix flakes&lt;/strong&gt; — deterministic build system; pins every dependency to a hash via &lt;code&gt;flake.lock&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Docker multi-stage build&lt;/strong&gt; — Stage 1 compiles the Nix toolchain inside a &lt;code&gt;nixos/nix&lt;/code&gt; container; Stage 2 drops it into a clean Debian image&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Telegram Bot API&lt;/strong&gt; — primary uplink channel; &lt;code&gt;dmPolicy: pairing&lt;/code&gt; means nobody talks to the agent without your explicit approval&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;just&lt;/strong&gt; — task runner wrapping all docker-compose and build commands into single-word ops&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;age&lt;/strong&gt; — encryption for secrets at rest&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;-architecture&#34;&gt;// ARCHITECTURE&lt;/h2&gt;
&lt;p&gt;The build is two-stage. Stage 1 spins a &lt;code&gt;nixos/nix&lt;/code&gt; container inside Docker and compiles the full toolchain — Node.js, Python, gcc, gh CLI, ripgrep, and the rest — with every package pinned by &lt;code&gt;flake.lock&lt;/code&gt;. Docker caches this layer aggressively; it only reruns when the Nix config changes. Stage 2 takes that compiled toolchain, drops it into a minimal Debian base, and installs OpenClaw and qwen-coder on top. The result is &lt;code&gt;nyx-cortex:latest&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;At runtime, two volumes bridge your Mac to the container: &lt;code&gt;secrets/&lt;/code&gt; mounts into &lt;code&gt;/config&lt;/code&gt; (OpenClaw watches it live — edit on your Mac, the agent reloads in seconds, no restart), and &lt;code&gt;data/&lt;/code&gt; mounts into &lt;code&gt;/data&lt;/code&gt; where all agent state lives — sessions, workspace files, gh auth tokens, agent sandboxes. The container is disposable. The data is not.&lt;/p&gt;
&lt;p&gt;Updates follow a strict three-layer hierarchy: config changes hot-reload instantly, OpenClaw/qwen-coder upgrades hit only Stage 2 (fast, cache hit on Stage 1), and Nix toolchain updates bump &lt;code&gt;flake.lock&lt;/code&gt; and trigger a full Stage 1 recompile. Even &lt;code&gt;just rebuild&lt;/code&gt; — scorched earth, no cache — leaves &lt;code&gt;/data&lt;/code&gt; untouched.&lt;/p&gt;
&lt;h2 id=&#34;-status&#34;&gt;// STATUS&lt;/h2&gt;
&lt;p&gt;Cortex is stable and in daily use. OpenClaw gateway runs on &lt;code&gt;:18789&lt;/code&gt; with optional LAN-accessible dashboard. Telegram pairing is operational; WhatsApp uplink is supported. Local inference via any OpenAI-compatible endpoint (Ollama, llama.cpp) wires in through the config. Multi-architecture builds confirmed on &lt;code&gt;aarch64-darwin&lt;/code&gt; (Apple Silicon) and &lt;code&gt;x86_64-linux&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;What&amp;rsquo;s next: tighter bubblewrap sandboxing on Linux for agent subprocesses, SBOM verification hooks in CI, and an optional WhatsApp-first config template.&lt;/p&gt;
&lt;h2 id=&#34;-deploy&#34;&gt;// DEPLOY&lt;/h2&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre tabindex=&#34;0&#34; style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;&#34;&gt;&lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# Install Nix (once)&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;curl -sSfL https://artifacts.nixos.org/nix-installer | sh -s -- install
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# Clone and enter dev shell&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;git clone https://github.com/netlooker/nyx.git
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;cd nyx
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;nix develop
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# Configure&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;cp secrets/openclaw.json5.example secrets/openclaw.json5
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;$EDITOR secrets/openclaw.json5
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# Bake and boot&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;just build
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;just up
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;just logs
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;div class=&#34;highlight&#34;&gt;&lt;pre tabindex=&#34;0&#34; style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;&#34;&gt;&lt;code class=&#34;language-bash&#34; data-lang=&#34;bash&#34;&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;just restart   &lt;span style=&#34;color:#75715e&#34;&gt;# swap container, keep data&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span style=&#34;display:flex;&#34;&gt;&lt;span&gt;just rebuild   &lt;span style=&#34;color:#75715e&#34;&gt;# scorched earth — still keeps /data&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;One command to build, one to run — the cortex either works or it tells you exactly why it doesn&amp;rsquo;t.&lt;/p&gt;
</content>
    </item>
    
  </channel>
</rss>
